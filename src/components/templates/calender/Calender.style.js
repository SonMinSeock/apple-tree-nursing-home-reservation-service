import styled from "styled-components";

const Layout = styled.div`
  max-width: 1300px;
  margin: 100px auto;
`;

const CalendarBox = styled.div`
  margin-top: 40px;
`;

export { Layout, CalendarBox };
